#addons21 needs to be manually installed.
- defaults:
        link:
                create: true
                relink: true
- clean: ['~','~/.config']

- link:
    ~/.profile: 
    ~/.bashrc: 
    ~/.bash_logout: 
    ~/.gitconfig: 
    ~/.ideavimrc:
    ~/.XCompose: 
    ~/.Xresources-regolith:
    ~/.xinitrc:
    ~/.zshrc:
    ~/.jupyter:
    ~/.tmux.conf:
    ~/.alacritty.yml:
    ~/.tridactylrc:
    ~/bin:
    ~/.config/:
            glob: true
            path: config/*
    ~/.shell:
            glob: true
            path: shell/*
    ~/.i3status.conf:
    ~/.doom.d:
    ~/.screenlayout:

- shell:
        - git submodule sync --recursive
        - git submodule update
