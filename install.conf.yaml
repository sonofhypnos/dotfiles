- defaults:
        link:
                create: true
                relink: true
- clean: ['~','~/.config']

- link:
    ~/.profile: 
    ~/.bashrc: 
    ~/.bash_logout: 
    ~/.gitconfig: 
    ~/.xini: 
    ~/.XCompose: 
    ~/.zshrc:
    ~/.tmux.conf:
    ~/.alacritty.yml:
    ~/.config/regolith/i3/config: config/i3/config
    ~/.config/:
            glob: true
            path: config/*
    ~/.shell:
    ~/.spacemacs:
    ~/.spacemacs.env:
    ~/.i3status.conf:


