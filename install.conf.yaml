#addons21 needs to be manually installed.
- defaults:
        link:
                create: true
                relink: true
- clean: ['~','~/.config']

- link:
    ~/.profile: 
    ~/.bashrc: 
    ~/.bash_logout: 
    ~/.gitconfig: 
    ~/.xini: 
    ~/.XCompose: 
    ~/.Xresources-regolith:
    ~/.zshrc:
    ~/.tmux.conf:
    ~/.alacritty.yml:
    ~/bin:
    ~/.config/:
            glob: true
            path: config/*
    ~/.shell:
    ~/.spacemacs:
    ~/.spacemacs.env:
    ~/.i3status.conf:
    ~/.emacs-profiles.el:

- shell:
        - git submodule sync --recursive
        - git submodule update
