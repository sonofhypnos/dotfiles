- defaults:
        link:
                create: true
                relink: true
- clean: ['~','~/.config']

- link:
    ~/.profile: 
    ~/.bashrc: 
    ~/.bash_logout: 
    ~/.gitconfig: 
    ~/.xini: 
    ~/.XCompose: 
    ~/.zshrc:
    ~/.tmux.conf:
    ~/.config/:
            glob: true
            path: config/*
    ~/.shell:


