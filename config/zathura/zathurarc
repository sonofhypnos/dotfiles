set window-title-basename "true"
set selection-clipboard "clipboard"
set selection-notification false #turns off the notification when you select text

set adjust-open width
#set recolor true
map <C-Z> feedkeys ":! zotadd % <Return>" #Adds currently open pdf to zotero.
